import LoadingScreen from '@/components/Loading';

export default function Loading() {
  return (
    <section className="loading-screen">
      <LoadingScreen title="Carregando detalhes do filme..." />
    </section>
  );
}
