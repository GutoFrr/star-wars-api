import LoadingScreen from '@/components/Loading';

export default function Loading() {
  return (
    <section className="loading-screen">
      <LoadingScreen title="Searching for characters..." />
    </section>
  );
}
